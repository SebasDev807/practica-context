export const InputErrorMessage =
    ({ children }: { children: React.ReactNode }) => {
        return (
            <p className="errorMessage">
                {children}
            </p>

        )
    }
